# this is an example of good simulation configuration

[aapl, goog, ibm]
|S mf = median_filter( window = 4, print_signals = false )
|B open = open( mf, process_all = true )
|P statistics = statistic( open, hello_world = "nowere" )
